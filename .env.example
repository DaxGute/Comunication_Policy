# Local secrets for the Vite OpenAI proxy (never commit).
# Copy to .env.local and fill in a real key:
#
#   cp .env.example .env.local
#
OPENAI_API_KEY=

# Optional OpenAI scheduler overrides (defaults target ~87% of per-model Tier 2 limits).
# OPENAI_RATE_MARGIN=0.87
# OPENAI_MAX_CONCURRENT=128
